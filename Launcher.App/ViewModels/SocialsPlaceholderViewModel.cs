using Launcher.App.Services;

namespace Launcher.App.ViewModels;

public class SocialsPlaceholderViewModel : ViewModelBase
{
    public SocialsPlaceholderViewModel(IMessengerService messengerService)
        : base(messengerService)
    {
    }
}
