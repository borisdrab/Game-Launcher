using Launcher.App.ViewModels;

namespace Launcher.App.Views;

public partial class SocialsPlaceholderPage : ContentPageBase
{
    public SocialsPlaceholderPage(SocialsPlaceholderViewModel viewModel)
        : base(viewModel)
    {
        InitializeComponent();
    }
}
